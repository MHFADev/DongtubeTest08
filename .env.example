# ===========================================
# DONGTUBE API - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# For Vercel: Add these in Project Settings -> Environment Variables

# ===========================================
# REQUIRED VARIABLES (Must be set for deployment)
# ===========================================

# JWT Secret - Used for authentication tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-replace-with-64-byte-hex

# PostgreSQL Database URL
# Format: postgresql://username:password@host:port/database?sslmode=require
# For Vercel Postgres: Get this from your Vercel dashboard
# For Neon: Get this from your Neon dashboard
# For Supabase: Get this from your Supabase dashboard
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# ===========================================
# OPTIONAL VARIABLES
# ===========================================

# Node environment (production, development, test)
NODE_ENV=production

# Server port (used in local development, ignored on Vercel)
PORT=5000

# ===========================================
# VERCEL DEPLOYMENT NOTES
# ===========================================
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings -> Environment Variables
# 3. Add JWT_SECRET and DATABASE_URL
# 4. Make sure to set them for Production, Preview, and Development
# 5. Redeploy after adding environment variables
